{
  "name": "ace-everyday",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:worker": "node scripts/build-worker.mjs",
    "test": "npm run build:worker && node --test tests/*.mjs"
  }
}
