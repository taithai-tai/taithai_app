# Server-side only. Configure the same name in Vercel Environment Variables.
GEMINI_API_KEY=

# Optional override. The app defaults to the stable multimodal model below.
GEMINI_MODEL=gemini-2.5-flash
